{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window. Kept intentionally narrow: the app is fully local and must not gain filesystem or network permissions it does not use.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
